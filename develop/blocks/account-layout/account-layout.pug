mixin account-layout()
    +b.account-layout
        +lines('account')
        +inner
            +e.wrapper
                +e.ASIDE.sidebar
                    +account-nav
                +e.MAIN.main
                    block